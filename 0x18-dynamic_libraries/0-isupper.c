#include "main.h"
/**
 * _isupper - this functions checks for upper case character
 * @c: this represents the character
 * Return: 1 if it is an uppercase and c if it s uppercase and 0 otherwise
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
	return (1);
	}
	else
	return (0);
}
