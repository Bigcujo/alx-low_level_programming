#include "main.h"
/**
 * mul - this multiplies two integer values
 * @a: this takes the first intger
 * @b: this takes the value of the second integer value
 * Return: it will return the value gotten from the mutiplication
 */
int mul(int a, int b)
{
	return (a * b);
}
